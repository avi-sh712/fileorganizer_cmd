# A file organizer using java

A cmd based file organizer which organizes all the files in a folder according to their extensions. 
The user has to enter the direcctory path and it organizes that directory.
